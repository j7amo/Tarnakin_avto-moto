export const getViewStatus = (state) => state.modal.viewStatus;

export const getReviews = (state) => state.reviews.reviews;

export const getModalRating = (state) => state.modal.modalRating;

export const getName = (state) => state.modal.name;

export const getAdvantages = (state) => state.modal.advantages;

export const getDisadvantages = (state) => state.modal.disadvantages;

export const getComment = (state) => state.modal.comment;
